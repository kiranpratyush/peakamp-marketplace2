const nextConfig = {}

const path = require('path')

module.exports = nextConfig

module.exports = {
    images: {
      remotePatterns: [
        {
          hostname: 'images.unsplash.com', 
        },
      ],
    },
}